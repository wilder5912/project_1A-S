import {Business} from '../business/Business';
import {Provider} from '../provider/Provider';
export class DetailProvider {
  detailProviderId: number;
  businessId: Business;
  providerId: Provider;
  isDelete: boolean;
}
