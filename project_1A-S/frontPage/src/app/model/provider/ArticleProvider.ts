import {Provider} from '../provider/Provider';
export class ArticleProvider {
  articleProviderId: number;
  codeArticleProvider: string;
  nameArticleProvider: string;
  providerId: Provider;
  isDelete: boolean;
}
