import { RoleUserIdPipe } from './role-user-id.pipe';

describe('RoleUserIdPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleUserIdPipe();
    expect(pipe).toBeTruthy();
  });
});
