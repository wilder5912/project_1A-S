import { NameTypeMoneyPipe } from './name-type-money.pipe';

describe('NameTypeMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new NameTypeMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
