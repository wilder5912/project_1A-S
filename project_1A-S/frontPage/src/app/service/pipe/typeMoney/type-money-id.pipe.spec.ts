import { TypeMoneyIdPipe } from './type-money-id.pipe';

describe('TypeMoneyIdPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeMoneyIdPipe();
    expect(pipe).toBeTruthy();
  });
});
