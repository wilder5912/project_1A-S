import { TypeChangeTypeMoneyPipe } from './type-change-type-money.pipe';

describe('TypeChangeTypeMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeChangeTypeMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
